name: Build & Test

on:
  push:
    branches: [ new_replayer ]
  pull_request:
    branches: [ new_replayer ]

jobs:
  build:

    runs-on: ubuntu-latest
    container:
      image: nokal/simod-testing:v1.0.4
